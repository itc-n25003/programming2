# 3-1 通信プログラムの基礎3

## 1)netcat代替のサンプルコードの動作確認

#### 教科書p.13からはじまる「2.5 netcatの代替」を読み、「2.5.1試してみる」まで読み進めたら、教科書の記述に従ってターミナル上でnetcat.pyを実行していきましょう。

（試してみるの注意）  
・自身のIPアドレスは、ip addr showで確認しましょう  
・最後のリクエスト送信は、echoの1行を書いてからenterキーを押し、下の行に＞が表示されてから残りの部分を書いてenterキーを押しましょう

## 2)subprocessを用いたLinuxコマンド実行プログラムの作成

#### 1のサンプルコードでは、subprocessというライブラリを使ってpythonのプログラム上でLinuxのコマンドを実行しています。それを使っている箇所を確認してみましょう。

#### また、subprocess.run("文字列”)で文字列にLinuxコマンドを記述すると、それを実行することができます。新たにプログラムを作成し、自分が知っているLinuxコマンドをPythonで実行する機能を実装してみましょう。

```python
# subpro_0513.py内コード
import subprocess                                                              
                                                                               
subprocess.run(["echo", "success"])                                            

```

```bash
(venv3) $ python3 subpro_0513.py 
success
```

## 3)課題提出

#### 2で作成したプログラムを提出してもらいます。

# 3-2 通信プログラムの基礎4

## 1)TCPプロキシプログラムの実行

#### 教科書p.22からはじまる「2.6 TCPプロキシ」を読みながらサンプルコードの概要を確認してください。「2.6.1試してみる」まで読み進めたら、教科書の記述に従ってターミナル上でproxy.pyを実行していきましょう。

（試してみるの注意）  
・仮想環境のPythonをsudoで使う場合は、以下のようにフルパスで指定する必要があります(今後も使うことがあるので覚えておきましょう）  
　sudo ~/bhp/venv3/bin/python [プログラム名]  
  
・ftp接続先のパスワードとユーザ名はどちらも「anonymous」です  
　＊接続先のタイムアウトが短いのでコピペしましょう  
・proxy.pyを動かしている端末で別の端末から入力した情報が確認できればOKです。

## 2)アスキーコードの解析

#### 1でproxy.pyを動かした端末の出力結果を観察してください。16進数表記の数字が並んでいる箇所があります。

例）32 32 30 20…

#### これらは、16進数の数値をアルファベットと対応させる文字コード（アスキーコード）です。アスキーコードの法則を調べ、16進数の文字コード（左）と文字（右）が正しく対応しているか確認しましょう。また、サンプルコードにおいて文字コードを出力する機能を実現している箇所を確認してみましょう。

```
#受信内容一部抜粋
[<==] Received 16 bytes from local.
0000  50 41 53 53 20 61 6E 6F 6E 79 6D 6F 75 73 0D 0A   PASS anonymous..
```

```bash
(venv3) $ python3
>>> "".join([chr(int(i,16)) for i in ["50", "41", "53", "53", "20", "61", "6E", "6F", "6E", "79", "6D", "6F", "75", "73", "0D", "0A" ]])
'PASS anonymous\r\n'
```

## 3)課題提出

#### 2の確認結果を提出してください。

